# cookbook/04_workflows/07_cel_expressions 的 TEST_LOG

生成时间：2026-02-08 16:39:09

### condition/cel_additional_data.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。┃ • Check for typos and grammatical errors. ┃

---

### condition/cel_basic.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 1.5s

---

### condition/cel_previous_step.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。DEBUG My API returns 500 errors when I send POST requests with JSON payloads.

---

### condition/cel_previous_step_outputs.py

**状态：** FAIL

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 35秒后超时。┃ • Implement robust error handling mechanisms within automated systems to ┃

---

### condition/cel_session_state.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。┃ previously. Understanding the specific error message can help us target ┃

---

### loop/cel_compound_exit.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。- **Accountability**: Determining liability when AI systems make errors in

---

### loop/cel_content_keyword.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 1.5s

---

### loop/cel_iteration_limit.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 6.0s

---

### loop/cel_step_outputs_check.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 10.9s

---

### router/cel_additional_data_route.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 1.8s

---

### router/cel_previous_step_route.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。DEBUG My API keeps returning 503 errors.

---

### router/cel_session_state_route.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 10.1s

---

### router/cel_ternary.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。Completed in 5.9s

---

### router/cel_using_step_choices.py

**状态：** PASS

**描述：** 使用 `.venvs/demo/bin/python` 执行（模式：normal，超时：35s）。

**结果：** 执行成功。┃ error correction, and fault-tolerant systems are crucial for realizing ┃

---
